
{
  "entities": {
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a single product available for sale in the inventory, managed via barcode or manual entry.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity. This is typically the EAN-13 barcode of the product."
        },
        "name": {
          "type": "string",
          "description": "The name or title of the product."
        },
        "price": {
          "type": "number",
          "description": "The current selling price of a single unit of the product."
        },
        "stock": {
          "type": "number",
          "description": "The available quantity of the product currently in stock."
        },
        "idealStock": {
          "type": "number",
          "description": "The desired amount of stock for this product to be considered in optimal state."
        },
        "warningStock": {
          "type": "number",
          "description": "A fixed threshold. If stock is below this number, the product is in danger state."
        },
        "category": {
          "type": "string",
          "description": "The category the product belongs to."
        },
        "distributor": {
          "type": "string",
          "description": "The name of the company or distributor providing the product."
        },
        "buyByCase": {
          "type": "boolean",
          "description": "Whether this product is typically restocked by buying a full case."
        },
        "unitsPerCase": {
          "type": "number",
          "description": "Number of individual units contained in a single case."
        }
      },
      "required": [
        "id",
        "name",
        "price",
        "stock"
      ]
    },
    "Sale": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Sale",
      "type": "object",
      "description": "Represents a completed sales transaction.",
      "properties": {
        "id": {
          "type": "string"
        },
        "saleDateTime": {
          "type": "string",
          "format": "date-time"
        },
        "totalAmount": {
          "type": "number"
        },
        "paymentMethod": {
          "type": "string",
          "enum": ["cash", "card", "deduction"],
          "description": "The method used for the payment. 'deduction' means stock removal without charge."
        },
        "productSaleItemIds": {
          "type": "array",
          "items": { "type": "string" }
        },
        "manualSaleItemIds": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": [
        "id",
        "saleDateTime",
        "totalAmount"
      ]
    },
    "InventoryLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "InventoryLog",
      "type": "object",
      "description": "Represents an entry of new stock into the inventory.",
      "properties": {
        "id": { "type": "string" },
        "productId": { "type": "string" },
        "productName": { "type": "string" },
        "quantity": { "type": "number" },
        "invoiceNumber": { "type": "string", "description": "The invoice number associated with this stock entry." },
        "timestamp": { "type": "string", "format": "date-time" },
        "type": { "type": "string", "enum": ["restock", "adjustment"] }
      },
      "required": ["id", "productId", "productName", "quantity", "timestamp"]
    },
    "BreadLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BreadLog",
      "type": "object",
      "description": "Daily record of bread purchases and remaining stock (measured in kg) to control sales/waste.",
      "properties": {
        "id": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "bought": { "type": "number", "description": "Amount of bread bought today in kg." },
        "remaining": { "type": "number", "description": "Amount of bread left today in kg." },
        "clima": { "type": "string", "enum": ["Sol", "Nubes", "Lluvia"], "description": "Weather of the day." },
        "quiebre": { "type": "boolean", "description": "Whether stock ran out prematurely." },
        "observation": { "type": "string", "description": "Optional notes." },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "date", "bought", "remaining"]
    },
    "Note": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Note",
      "type": "object",
      "description": "A simple note or reminder.",
      "properties": {
        "id": { "type": "string" },
        "content": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "content", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/authorizedUsers/{userId}",
        "definition": {
          "entityName": "User",
          "description": "Authorized users of the POS system."
        }
      },
      {
        "path": "/products/{productId}",
        "definition": {
          "entityName": "Product",
          "description": "Main inventory collection."
        }
      },
      {
        "path": "/sales/{saleId}",
        "definition": {
          "entityName": "Sale",
          "description": "Completed sales transactions."
        }
      },
      {
        "path": "/inventoryLogs/{logId}",
        "definition": {
          "entityName": "InventoryLog",
          "description": "Logs for stock additions and adjustments."
        }
      },
      {
        "path": "/breadLogs/{logId}",
        "definition": {
          "entityName": "BreadLog",
          "description": "Daily bread control logs (in kg)."
        }
      },
      {
        "path": "/notes/{noteId}",
        "definition": {
          "entityName": "Note",
          "description": "System reminders and notes."
        }
      }
    ]
  }
}
